{
  "name": "@hhgoa/eval",
  "private": true,
  "version": "0.1.0",
  "description": "WS7: eval and export (backtest.ts, runBenchmark.ts, exportAnswers.ts, validateAnswers.ts, metrics.ts).",
  "type": "module",
  "scripts": {
    "run-benchmark": "tsx src/runBenchmark.ts",
    "run-case": "tsx src/runBenchmark.ts --case-only",
    "validate-answers": "tsx src/validate.ts",
    "backtest": "tsx src/backtest.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hhgoa/agent": "workspace:*",
    "@hhgoa/contracts": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.23.13",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}