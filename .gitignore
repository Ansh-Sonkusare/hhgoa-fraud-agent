# Dependencies
node_modules/

# tigergraph-mcp Python venv (CLAUDE.md Stack: isolated to graph/mcp-server/, own venv/deps)
graph/mcp-server/venv/

# Raw dataset (never committed — see PRD §16, README "Rules")
data/
*.csv
!api/src/data/

# Secrets
.env
.env.local

# Build / runtime artifacts
dist/
build/
*.tsbuildinfo
.cache/
.turbo/
runs/
coverage/
.next/

# Editor / OS
.DS_Store
*.log

# Worktree management (agent-created git worktrees under .claude/)
.claude/

# graft code-graph index (local tool artifact, regenerated on demand)
graft/

# Backtest outputs (eval/src/backtest.ts writes one pair per run/shard tag;
# regenerated on demand and they embed case data derived from data/)
eval/NOTES*.md
eval/backtest-detail*.json
